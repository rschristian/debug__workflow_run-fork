name: Reporter Alt

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - requested
      - completed

jobs:
  reporter-alt:
    name: Reporter Alt
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - run: echo "Reporter"
